name: test workflow 

on:
  workflow_dispatch:

jobs:
  runs-on: ubuntu-latest

  steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Sed Test
      run: | 
        sed -i "s|VPS|VPST|" README.md
        cat README.md

  
